import { MillisToIsoPipe } from './millis-to-iso.pipe';

describe('MillisToIsoPipe', () => {
  it('create an instance', () => {
    const pipe = new MillisToIsoPipe();
    expect(pipe).toBeTruthy();
  });
});
